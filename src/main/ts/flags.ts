const FLAG_PRODUCTION = true;
const FLAG_PRODUCTION_MINIMAL = false && FLAG_PRODUCTION;

const FLAG_SHOW_FPS = true && !FLAG_PRODUCTION; 
const FLAG_GL_SHOW_SHADER_ERRORS = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_GL_CULL_FACES = true;
const FLAG_GL_CULL_EXPLOSIONS = false;
const FLAG_GL_DISABLE_BLENDING = false;
const FLAG_GL_16_BIT_INDEXES = true;
const FLAG_CAP_ENDS = true;
const FLAG_CLEAN_UP_ON_RESIZE = !FLAG_PRODUCTION_MINIMAL;
const FLAG_CLEANUP_EVENT_HANDLERS_ON_DESTROY = false && !FLAG_PRODUCTION_MINIMAL;
const FLAG_MUZZLE_FLASH = true;
const FLAG_DEATH_ANIMATION = true;
const FLAG_ALLOW_JUMPING = true;
const FLAG_SHAKY_CAMERA = true;
const FLAG_CLEAN_UP_ENTITY = true;
const FLAG_CHECK_INVALID_START_POSITION = true;
const FLAG_WARN_INVALID_START_POSITION = true && !FLAG_PRODUCTION;
const FLAG_FOLLOW_CAMERA = true;

const FLAG_CHECK_FOR_BAD_BUFFERS = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_LOG_KILLS = true && !FLAG_PRODUCTION;
const FLAG_COLLIDE_WITH_SIDE = true;
const FLAG_CHECK_MAX_VELOCITY = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_WARN_MAX_VELOCITY_EXCEEDED = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_SPHERE_PHYSICS = false;
const FLAG_TEST_PHYSICS = false && !FLAG_PRODUCTION_MINIMAL;
const FLAG_DETECT_MULTIPLE_COLLISIONS = true && !FLAG_PRODUCTION;
const FLAG_SPAWN_RANDOM_MINIBOSSES = true;
const FLAG_BACKGROUND = true;
const FLAG_START_AT_LAST_LIBERATED = true;
const FLAG_SPEECH = true;
const FLAG_BACKGROUND_TARGET = true;
const FLAG_WARN_SHIFT_TOO_FAR = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_BLOOM = true;
const FLAG_HOME_SCREEN = false;
const FLAG_FLASH_BATTERY_BOOST_FLASH_BAR = true;
const FLAG_SHORTEN_GLSL_VARIABLES = false || FLAG_PRODUCTION_MINIMAL;
const FLAG_NO_LOADING = true;
const FLAG_MINIMAL_AUDIO_CLEANUP = false;
const FLAG_PROPERLY_CHECK_SURFACE_NORMAL = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_BIGGER_IS_BRIGHTER = true;
const FLAG_USE_GL_CONSTANTS = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_AUDIO_SET_DISTANCE_MODEL_EXPONENTIAL = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_PRONOUCE_PROPERLY = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_SHIELD_EFFECTS_FOCUS = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_PERSISTENT_SEED = true;
const FLAG_CHECK_SPEECH = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_CHECK_WEBKIT_AUDIO_CONTEXT = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_MEASURE_TEXT = true && !FLAG_PRODUCTION_MINIMAL; 
const FLAG_BEHAVIOUR_DIE = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_BEHAVIOUR_CHECK_ELEVATION = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_BEHAVIOUR_JUMP = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_BEHAVIOUR_FOLLOW = true && !FLAG_PRODUCTION_MINIMAL;
const FLAG_ASCII_BATTERY_SYMBOL = false && FLAG_PRODUCTION_MINIMAL;
const FLAG_INLINE_CROSS_PRODUCT = FLAG_PRODUCTION_MINIMAL;
const FLAG_INLINE_PROJECTION_MATRIX = FLAG_PRODUCTION_MINIMAL;
const FLAG_INLINE_IDENTITY_MATRIX = FLAG_PRODUCTION_MINIMAL;
const FLAG_USE_PI = true;